# Модуль 1 — Язык программирования Go

**Студент:** Воронков Артём Сергеевич  
**Преподаватель:** Непейвода Антонина Николаевна

---

## Содержание

- [Быстрая сортировка](#быстрая-сортировка)
- [Кодирование и раскодирование UTF-8](#кодирование-и-раскодирование-utf-8)
- [«Длинное» сложение](#длинное-сложение)
- [Решение СЛАУ в рациональных числах](#решение-слау-в-рациональных-числах)
- [Вычисление выражений в польской записи](#вычисление-выражений-в-польской-записи)
- [Лексический анализ](#лексический-анализ)
- [Арифметическое выражение](#арифметическое-выражение)

---

## Быстрая сортировка

**Файл:** [qsmsort.go](qsmsort.go)

Реализуйте алгоритм нерекурсивной быстрой сортировки произвольных данных в функции:

```go
func qssort(n int,
        less func(i, j int) bool,
        swap func(i, j int)) {
    ...
}
```

В качестве параметров функция `qsmsort` должна принимать:

- `n` — количество сортируемых записей
- `less` — функцию сравнения *i*-й и *j*-й записи
- `swap` — функцию обмена *i*-й и *j*-й записи

Функция должна использовать явный стек для хранения сортируемых диапазонов. Глубину стека допустимо делать O(n), т.е. оптимизацию логарифмической глубины делать не обязательно.

Составьте программу `qsmsort.go`, демонстрирующую работоспособность функции `qsmsort`.

---

## Кодирование и раскодирование UTF-8

**Файл:** [utf8.go](utf8.go)

Реализуйте алгоритмы перевода текста из UTF-32 в UTF-8 и обратно. Алгоритмы должны быть оформлены в виде двух функций:

```go
func encode(utf32 []rune) []byte {
    ...
}

func decode(utf8 []byte) []rune {
    ...
}
```

Функция `encode` принимает в качестве параметра текст в виде массива кодовых точек и возвращает образ этого текста в UTF-8. Функция `decode` выполняет обратное преобразование.

**Кодирование символов Unicode в UTF-8:**

| Код символа в Unicode | Байт 1 | Байт 2 | Байт 3 | Байт 4 |
|---|---|---|---|---|
| `0 0000 0000 0000 0xxx xxxx` | `0xxx xxxx` | — | — | — |
| `0 0000 0000 0yyy yyxx xxxx` | `110y yyyy` | `10xx xxxx` | — | — |
| `0 0000 zzzz yyyy yyxx xxxx` | `1110 zzzz` | `10yy yyyy` | `10xx xxxx` | — |
| `u uuzz zzzz yyyy yyxx xxxx` | `1111 0uuu` | `10zz zzzz` | `10yy yyyy` | `10xx xxxx` |

Составьте программу `utf8.go`, демонстрирующую работоспособность функций `decode` и `encode`. Проверьте правильность результатов работы функций с помощью встроенных средств Go.

---

## «Длинное» сложение

**Файл:** [add.go](add.go)

Реализуйте алгоритм сложения натуральных чисел, представленных в виде массива цифр в системе счисления по основанию *p*, где 1 < *p* < 2³⁰. Порядок цифр в массиве — little-endian (младшая цифра располагается в нулевом элементе массива).

```go
func add(a, b []int32, p int) []int32 {
    ...
}
```

Составьте программу `add.go`, демонстрирующую работоспособность функции `add`.

---

## Решение СЛАУ в рациональных числах

**Файл:** [gauss.go](gauss.go)

Составьте программу `gauss.go`, выполняющую решение системы линейных алгебраических уравнений в рациональных числах методом Гаусса.

**Формат входных данных:**

Программа должна считывать из входного потока число N уравнений (1 ≤ N ≤ 5) и матрицу системы размером N × (N+1). Матрица содержит целые числа от -9 до 9.

**Формат результата работы программы:**

Если система не имеет решения или имеет бесконечное количество решений, следует выводить фразу `No solution`.

Если решение существует, программа должна печатать в выходной поток N значений переменных, каждое из которых представляет собой нормализованное рациональное число, записанное в виде `n/d`, где n — числитель дроби, d — знаменатель дроби.

**Пример:**

Входные данные:
```
3
-4 -1  8  2
 7 -7  7  3
 5 -1 -4  7
```

Выходные данные:
```
377/21
214/7
274/21
```

---

## Вычисление выражений в польской записи

**Файл:** [polish.go](polish.go)

Польская запись — это форма записи арифметических, логических и алгебраических выражений, в которой операция располагается слева от операндов. Выражения в польской записи могут обходиться без скобок, однако скобки оставлены для наглядности.

Например, выражение `5 · (3 + 4)` в польской записи выглядит как:

```
(* 5 (+ 3 4))
```

Выражения состоят из чисел от 0 до 9, круглых скобок и трёх знаков операций: плюс, минус и звёздочка (умножить).

Требуется составить программу `polish.go`, вычисляющую значение выражения.

---

## Экономное вычисление выражений в польской записи

**Файл:** [econom.go](econom.go)

Пусть выражения в польской записи состоят из имён переменных (от a до z), круглых скобок и трёх знаков операций: #, $ и @ (смысл операций мы определять не будем).

Выражения могут содержать повторяющиеся подвыражения. Экономное вычисление таких выражений подразумевает, что повторяющиеся подвыражения вычисляются только один раз.

Требуется составить программу `econom.go`, вычисляющую количество операций, которые нужно выполнить для экономного вычисления выражения. Примеры работы программы приведены в таблице:

Набор тестов для программы экономного вычисления выражений в польской записи
| Выражение	| Количество операций |
| x	| 0 |
| ($xy) |	1 |
| ($(@ab)c) |	2 |
| (#i($jk)) |	2 |
| (#($ab)($ab)) |	2 |
| (@(#ab)($ab))	| 3 |
| (#($a($b($cd)))(@($b($cd))($a($b($cd))))) |	5 |
| (#($(#xy)($(#ab)(#ab)))(@z($(#ab)(#ab))))	| 6 |

---

## Лексический анализ

**Файл:** [lex.go](lex.go)

**Идентификатор** — это последовательность латинских букв и цифр, начинающаяся с буквы.

В некоторой строке записаны идентификаторы, разделённые произвольным количеством пробелов. Строка может начинаться и заканчиваться произвольным количеством пробелов. Такая строка называется **предложением**.

Лексический анализ предложения заключается в выделении из него последовательности идентификаторов и сопоставлении каждому уникальному идентификатору уникального целого числа, а одинаковым — одинакового.

**Пример:** для предложения
```
alpha x1 beta alpha x1 y
```
лексический анализатор может вернуть:
```
1 2 3 1 2 4
```

Необходимо разработать функцию `lex`:

```go
func lex(sentence string, array AssocArray) []int {
    ...
}
```

Ассоциативный массив должен реализовывать интерфейс `AssocArray`:

```go
type AssocArray interface {
    Assign(s string, x int)
    Lookup(s string) (x int, exists bool)
}
```

- `Assign` — добавляет в массив словарную пару
- `Lookup` — ищет пару по ключу; если не найдена, `exists = false`

Составьте программу `1ex.go`, демонстрирующую работоспособность функции `lex`. В качестве ассоциативных массивов для тестирования использовать **список с пропусками** и **АВЛ-дерево**:

```go
func makeSkipList() AssocArray {
    ...
}

func makeAVL() AssocArray {
    ...
}
```

---

## Арифметическое выражение

**Файл:** [arith.go](arith.go)

Составьте программу `arith.go`, вычисляющую значение скобочного арифметического выражения.

Арифметическое выражение считывается из аргументов командной строки и содержит целые числа от 0 до 2³¹, знаки четырёх арифметических операций, имена переменных, круглые скобки и пробелы. Арифметические операции — целочисленные (5/2 = 2). Пробелы игнорируются.

**Примеры выражений:**
```
-x * (x+10) * (128/x-5)
Length * Height * Depth
```

**Грамматика:**
```
<E>  ::= <E> + <T> | <E> - <T> | <T>
<T>  ::= <T> * <F> | <T> / <F> | <F>
<F>  ::= <number> | <var> | ( <E> ) | - <F>
```

**LL(1)-грамматика (после удаления левой рекурсии):**
```
<E>   ::= <T> <E'>
<E'>  ::= + <T> <E'> | - <T> <E'> | .
<T>   ::= <F> <T'>
<T'>  ::= * <F> <T'> | / <F> <T'> | .
<F>   ::= <number> | <var> | ( <E> ) | - <F>
```

Алгоритм вычисления разбит на две части: **лексический анализатор** и **синтаксический анализатор**.

Лексический анализатор выделяет лексемы — числовые константы, имена переменных, знаки операций и скобки. Каждая лексема представляется структурой:

```go
type Lexem struct {
    Tag
    Image string
}

type Tag int

const (
    ERROR  Tag = 1 << iota  // Неправильная лексема
    NUMBER                   // Целое число
    VAR                      // Имя переменной
    PLUS                     // Знак +
    MINUS                    // Знак -
    MUL                      // Знак *
    DIV                      // Знак /
    LPAREN                   // Левая круглая скобка
    RPAREN                   // Правая круглая скобка
)
```

Коды лексем — степени двойки для эффективной проверки принадлежности множеству:

```go
if lx.Tag & (PLUS | MINUS) != 0 {
    ...
}
```

Лексический анализатор запускается отдельной горутиной и отправляет лексемы в канал:

```go
func lexer(expr string, lexems chan Lexem) {
    ...
}
```

Синтаксический анализатор составляется методом **рекурсивного спуска** по LL(1)-грамматике.

Программа запрашивает у пользователя значения переменных и выводит результат или `error` при синтаксической ошибке. Если переменная входит в выражение несколько раз, её значение запрашивается только один раз.